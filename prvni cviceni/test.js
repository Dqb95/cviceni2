const levaStrana = 10;
const pravaStrana = 15;
const operator = '+';

function plus (a, b) {
    return a + b;
}

function pocitej()
let sum = 0;
switch(operator) {
    case '+':
        sum = plus(levaStrana, pravaStrana);
        brake;
    case '-':
        sum = minus(levaStrana, pravaStrana);
        brake;
    case '*':
        brake;
    case '/':
        brake;
}


console.log(sum);